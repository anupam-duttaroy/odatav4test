_schema-version: "3.2"
ID: posttestapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: posttestapp-destination-content
  type: com.sap.application.content
  requires:
  - name: posttestapp-destination-service
    parameters:
      content-target: true
  - name: posttestapp-repo-host
    parameters:
      service-key:
        name: posttestapp-repo-host-key
  - name: posttestapp-uaa
    parameters:
      service-key:
        name: posttestapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: posttestapp_html_repo_host
          ServiceInstanceName: posttestapp-html5-srv
          ServiceKeyName: posttestapp-repo-host-key
          sap.cloud.service: posttestapp
        - Authentication: OAuth2UserTokenExchange
          Name: posttestapp_uaa
          ServiceInstanceName: posttestapp-xsuaa-srv
          ServiceKeyName: posttestapp-uaa-key
          sap.cloud.service: posttestapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: posttestapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: posttestapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - posttestapp.zip
      name: posttestapp
      target-path: resources/
- name: posttestapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: posttestapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: posttestapp-destination-service
    service-plan: lite
- name: posttestapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: posttestapp-xsuaa-srv
    service-plan: application
- name: posttestapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: posttestapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
